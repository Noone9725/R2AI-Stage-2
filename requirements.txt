# ── R2AI Stage 2 — Financial Table Retrieval & Text-to-Pandas ──
#
#   pip install -r requirements.txt
#
# LƯU Ý CHO GPU LOCAL: Cài đặt PyTorch CUDA tương thích với máy của bạn:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# ── Core: bắt buộc cho mọi stage ─────────────────────────────
pandas>=2.2.0
numpy>=1.26.0
PyYAML>=6.0
python-dotenv>=1.0.0
tqdm>=4.66.0
huggingface-hub>=0.24.0

# ── Model & Inference: Dense retrieval + Quantization + LLM ──
torch>=2.4.0
transformers>=4.44.0
accelerate>=0.26.0
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
autoawq>=0.2.0
gptqmodel>=1.0.0
bitsandbytes>=0.43.0
protobuf>=5.26.1,<6.0.0dev

# ── Serving LLM / Remote API (Tùy chọn) ──────────────────────
openai>=1.40.0

# ── Testing ──────────────────────────────────────────────────
pytest>=8.0.0
