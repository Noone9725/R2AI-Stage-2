# ── R2AI Stage 2 — Financial Table Retrieval & Text-to-Pandas ──
#
#   pip install -r requirements.txt
#
# Nhom theo muc dich: co the chay Stage 1 (corpus) + BM25 retrieval chi
# voi nhom "core", khong can GPU. Dense retrieval va LLM can nhom "model".
# Xem README.md muc "Cai dat toi thieu".

# ── core: bat buoc cho moi stage ─────────────────────────────
pandas==2.3.3
numpy==2.3.5
PyYAML==6.0.3
python-dotenv==1.2.2
tqdm==4.69.0
# huggingface_hub: tai kho ViFinQA + bo cau hoi (scripts/00_fetch_data.py).
# Doc HF_TOKEN tu .env, khong bao gio ghi token vao config/log.
huggingface-hub==0.36.2

# ── model: dense retrieval + rerank + sinh code ──────────────
# torch: chon dung bien the CUDA cho may cua ban truoc khi cai
#        (https://pytorch.org/get-started/locally/)
torch==2.5.1
transformers==4.46.3
sentence-transformers==3.3.1
faiss-cpu==1.9.0

# ── serving LLM ──────────────────────────────────────────────
# vllm: chi can khi llm.backend = "vllm" (Linux + CUDA). Windows dung
#       backend "transformers". Bo comment neu can.
# vllm==0.6.4
#
# openai: client cho backend "openai" — TRO TOI endpoint self-host
#         (vLLM/TGI OpenAI-compatible). Model dong (GPT-4o, Gemini...)
#         BI CAM theo the le: chi duoc dung open-weight <=14B.
openai==1.59.6

# ── test ─────────────────────────────────────────────────────
pytest==8.3.4
